import React from 'react';
import { Text } from 'react-native';
import { View } from 'react-native';

interface itemProps {
    item:any
}
const RoundItem: React.FC<itemProps> = (item) => {
  return (
    <View>
        <Text>dsfds</Text>
    </View>
  );
};

export default RoundItem;
