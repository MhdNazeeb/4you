interface filterNames {
    topPick: string;
    favorites: string;
    nearest: string;
    latest: string;
}

const filterData: filterNames[] = [
    { topPick: "Top Pick", favorites: "Favorites", nearest: "Nearest", latest: "Latest" }
];

export default filterData